# Agent Memory: Context

This project has a Python FastAPI backend, a Playwright execution layer, and a Vite React dashboard.

The agent harness is a control plane for drafts, validation, dry-runs, audit events, and gated publish requests.
